{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "pipenv shell && pipenv install djangorestframework && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn mygallery.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}